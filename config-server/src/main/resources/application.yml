server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Hrishikesh-Girnekar/food-delivery-config.git
          default-label: main
          clone-on-start: true